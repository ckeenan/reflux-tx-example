// Modules
import React from 'react/addons';

// Components
import Navigation from './Navigation.jsx';

export default React.createClass({
  render() {
    return (
      <header>
        <h1>H1 Title</h1>
        <Navigation/>
      </header>
    );
  }
});
